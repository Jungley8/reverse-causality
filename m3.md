# 🎨《逆果溯因》UI 美化方案评估与升级

## 基于成熟游戏标准的完整设计指南

---

# 📊 一、当前方案评估

## ✅ 优点分析

| 方面         | 评价 | 说明                       |
| ------------ | ---- | -------------------------- |
| **系统架构** | ⭐⭐⭐⭐ | Theme资源化思路正确        |
| **配色基础** | ⭐⭐⭐  | 冷色系符合定位，但缺乏层次 |
| **组件覆盖** | ⭐⭐⭐⭐ | 覆盖了主要UI组件           |
| **实施计划** | ⭐⭐⭐⭐ | 步骤清晰，可操作性强       |

## ⚠️ 问题与不足

### 1. **配色方案过于简单**
```
当前配色：
背景：#0E1117  ← 单一背景色
主文本：#E6EDF3
强因果：#3FB950
弱因果：#D29922
错误：#F85149
```

**问题**：
- 缺少层次感（前景/中景/背景）
- 没有中性灰色梯度
- 缺少强调色/点缀色
- 功能性颜色不完整（警告/信息/禁用）

### 2. **缺少视觉层次设计**
- 没有定义Z轴层级（背景层/内容层/浮动层/弹窗层）
- 缺少阴影/光效系统
- 没有间距规范（Spacing System）

### 3. **字体系统不完整**
- 只定义了"统一18px"，缺少字阶（Type Scale）
- 没有字重（Weight）规范
- 缺少行高/字间距标准

### 4. **交互状态设计浅显**
- 只有Normal/Hover/Pressed/Disabled四态
- 缺少Loading/Success/Error等反馈状态
- 没有定义状态转换动画时长

---

# 🎨 二、成熟游戏级别的配色系统

## 2.1 扩展配色方案（Dark Academic 主题）

### 核心色板（Primary Colors）

```gdscript
# themes/ColorPalette.gd
class_name ColorPalette
extends Resource

# === 背景层级（Backgrounds） ===
const BG_BASE = Color("#0A0D12")        # 最深背景
const BG_SURFACE = Color("#0E1117")     # 主要表面
const BG_ELEVATED = Color("#161A23")    # 抬升表面
const BG_OVERLAY = Color("#1E232E")     # 叠加层

# === 边框/分割线（Borders） ===
const BORDER_SUBTLE = Color("#1C2128")   # 微弱边框
const BORDER_DEFAULT = Color("#2D3139")  # 默认边框
const BORDER_STRONG = Color("#3D4149")   # 强边框
const BORDER_ACCENT = Color("#484F58")   # 强调边框

# === 文本层级（Text） ===
const TEXT_PRIMARY = Color("#E6EDF3")    # 主要文本
const TEXT_SECONDARY = Color("#9EA7B3")  # 次要文本
const TEXT_TERTIARY = Color("#6E7781")   # 三级文本
const TEXT_DISABLED = Color("#484F58")   # 禁用文本
const TEXT_INVERSE = Color("#0A0D12")    # 反色文本（用于强调按钮）

# === 功能色（Functional） ===
# 成功/因果强
const SUCCESS_DEFAULT = Color("#3FB950")
const SUCCESS_EMPHASIS = Color("#4AC45A")  # 悬停
const SUCCESS_MUTED = Color("#2D8A3E")     # 弱化
const SUCCESS_SUBTLE = Color("#1C4D2B")    # 背景

# 警告/因果中等
const WARNING_DEFAULT = Color("#D29922")
const WARNING_EMPHASIS = Color("#E5A82E")
const WARNING_MUTED = Color("#9E7419")
const WARNING_SUBTLE = Color("#4A3410")

# 错误/危险
const DANGER_DEFAULT = Color("#F85149")
const DANGER_EMPHASIS = Color("#FF6B62")
const DANGER_MUTED = Color("#C93A31")
const DANGER_SUBTLE = Color("#4A1C18")

# 信息/中性
const INFO_DEFAULT = Color("#58A6FF")
const INFO_EMPHASIS = Color("#79C0FF")
const INFO_MUTED = Color("#388BFD")
const INFO_SUBTLE = Color("#0D3A66")

# === 点缀色（Accent） ===
const ACCENT_CYAN = Color("#76E3EA")      # 科技感
const ACCENT_PURPLE = Color("#BC8CFF")    # 神秘感
const ACCENT_GOLD = Color("#E3B341")      # 高级感（S级评价）

# === 半透明层（Overlays） ===
const OVERLAY_DARK_10 = Color(0, 0, 0, 0.1)
const OVERLAY_DARK_30 = Color(0, 0, 0, 0.3)
const OVERLAY_DARK_50 = Color(0, 0, 0, 0.5)
const OVERLAY_DARK_80 = Color(0, 0, 0, 0.8)

const OVERLAY_LIGHT_10 = Color(1, 1, 1, 0.1)
const OVERLAY_LIGHT_20 = Color(1, 1, 1, 0.2)
const OVERLAY_LIGHT_30 = Color(1, 1, 1, 0.3)
```

### 语义化颜色映射

```gdscript
# === 组件颜色映射 ===
# CauseCard
const CARD_BG_NORMAL = BG_ELEVATED
const CARD_BG_HOVER = BG_OVERLAY
const CARD_BG_ACTIVE = BG_SURFACE
const CARD_BORDER_NORMAL = BORDER_DEFAULT
const CARD_BORDER_HOVER = BORDER_ACCENT
const CARD_BORDER_FOCUS = INFO_DEFAULT

# ChainSlot
const SLOT_BG_EMPTY = BG_SURFACE
const SLOT_BG_VALID = SUCCESS_SUBTLE
const SLOT_BG_INVALID = DANGER_SUBTLE
const SLOT_BORDER_EMPTY = BORDER_SUBTLE
const SLOT_BORDER_VALID = SUCCESS_DEFAULT
const SLOT_BORDER_INVALID = DANGER_DEFAULT

# Button
const BUTTON_PRIMARY_BG = INFO_DEFAULT
const BUTTON_PRIMARY_HOVER = INFO_EMPHASIS
const BUTTON_PRIMARY_ACTIVE = INFO_MUTED
const BUTTON_SECONDARY_BG = BG_ELEVATED
const BUTTON_SECONDARY_HOVER = BG_OVERLAY
```

---

## 2.2 字体系统（Type Scale）

```gdscript
# themes/TypographySystem.gd
class_name TypographySystem
extends Resource

# === 字阶定义 ===
const FONT_SIZE_DISPLAY = 48      # 大标题（主菜单游戏名）
const FONT_SIZE_H1 = 32           # 一级标题
const FONT_SIZE_H2 = 24           # 二级标题
const FONT_SIZE_H3 = 20           # 三级标题
const FONT_SIZE_BODY_LARGE = 18   # 正文大
const FONT_SIZE_BODY = 16         # 正文
const FONT_SIZE_BODY_SMALL = 14   # 正文小
const FONT_SIZE_CAPTION = 12      # 说明文字

# === 字重 ===
# 注：Godot需要导入不同字重的字体文件
const WEIGHT_LIGHT = "Light"      # 300
const WEIGHT_REGULAR = "Regular"  # 400
const WEIGHT_MEDIUM = "Medium"    # 500
const WEIGHT_SEMIBOLD = "SemiBold" # 600
const WEIGHT_BOLD = "Bold"        # 700

# === 行高系数 ===
const LINE_HEIGHT_TIGHT = 1.2     # 紧凑（标题）
const LINE_HEIGHT_NORMAL = 1.5    # 正常（正文）
const LINE_HEIGHT_RELAXED = 1.8   # 宽松（长文本）

# === 字间距 ===
const LETTER_SPACING_TIGHT = -0.5
const LETTER_SPACING_NORMAL = 0.0
const LETTER_SPACING_WIDE = 1.0
```

### 推荐字体

**方案A：开源免费方案**
```
主字体：Inter (Variable Font)
- 下载：https://github.com/rsms/inter
- 特点：极佳的屏幕显示效果，支持可变字重

代码/等宽：JetBrains Mono
- 下载：https://www.jetbrains.com/lp/mono/
- 用途：因果节点ID、调试信息
```

**方案B：Godot 内置字体增强**
```gdscript
# 使用系统字体 + 自定义配置
var font = SystemFont.new()
font.font_names = ["Arial", "Helvetica", "Sans-Serif"]
font.antialiasing = TextServer.FONT_ANTIALIASING_LCD
font.subpixel_positioning = TextServer.SUBPIXEL_POSITIONING_AUTO
```

---

## 2.3 阴影与光效系统

```gdscript
# themes/ShadowSystem.gd
class_name ShadowSystem
extends Resource

# === 阴影层级 ===
# 用于定义UI元素的"悬浮"高度

# Level 0: 贴合表面（无阴影）
static func shadow_none() -> Dictionary:
	return {}

# Level 1: 微弱抬升（按钮默认、卡片）
static func shadow_sm() -> Dictionary:
	return {
		"shadow_color": Color(0, 0, 0, 0.15),
		"shadow_size": 2,
		"shadow_offset": Vector2(0, 1)
	}

# Level 2: 中度抬升（悬停状态、下拉菜单）
static func shadow_md() -> Dictionary:
	return {
		"shadow_color": Color(0, 0, 0, 0.25),
		"shadow_size": 4,
		"shadow_offset": Vector2(0, 2)
	}

# Level 3: 高度抬升（模态框、弹窗）
static func shadow_lg() -> Dictionary:
	return {
		"shadow_color": Color(0, 0, 0, 0.35),
		"shadow_size": 8,
		"shadow_offset": Vector2(0, 4)
	}

# Level 4: 极高抬升（通知、Toast）
static func shadow_xl() -> Dictionary:
	return {
		"shadow_color": Color(0, 0, 0, 0.45),
		"shadow_size": 16,
		"shadow_offset": Vector2(0, 8)
	}

# === 内阴影（用于凹陷效果） ===
static func shadow_inner() -> Dictionary:
	return {
		"shadow_color": Color(0, 0, 0, 0.2),
		"shadow_size": -2,  # 负值表示内阴影
		"shadow_offset": Vector2(0, -1)
	}

# === 光晕效果 ===
static func glow_success() -> Dictionary:
	return {
		"shadow_color": ColorPalette.SUCCESS_DEFAULT.lightened(0.2),
		"shadow_size": 8,
		"shadow_offset": Vector2(0, 0)
	}

static func glow_danger() -> Dictionary:
	return {
		"shadow_color": ColorPalette.DANGER_DEFAULT.lightened(0.2),
		"shadow_size": 8,
		"shadow_offset": Vector2(0, 0)
	}
```

---

## 2.4 间距系统（Spacing Scale）

```gdscript
# themes/SpacingSystem.gd
class_name SpacingSystem
extends Resource

# === 基础单位 ===
const UNIT = 4  # 所有间距都是4的倍数

# === 间距等级 ===
const SPACING_NONE = 0
const SPACING_XXS = 4      # 1 unit
const SPACING_XS = 8       # 2 units
const SPACING_SM = 12      # 3 units
const SPACING_MD = 16      # 4 units （默认）
const SPACING_LG = 24      # 6 units
const SPACING_XL = 32      # 8 units
const SPACING_XXL = 48     # 12 units
const SPACING_XXXL = 64    # 16 units

# === 组件特定间距 ===
const CARD_PADDING = SPACING_MD
const CARD_MARGIN = SPACING_SM
const BUTTON_PADDING_H = SPACING_LG  # 水平内边距
const BUTTON_PADDING_V = SPACING_SM  # 垂直内边距
const PANEL_PADDING = SPACING_LG
const GRID_GAP = SPACING_MD
```

---

# 🎯 三、完整主题资源实现

## 3.1 创建 default_theme.tres

由于Godot的Theme资源文件无法直接用文本展示，这里提供**通过脚本生成主题**的方案：

```gdscript
# themes/ThemeBuilder.gd
@tool
class_name ThemeBuilder
extends Node

# 在编辑器中运行此脚本生成主题文件

func build_theme() -> Theme:
	var theme = Theme.new()
	
	# === 字体设置 ===
	_setup_fonts(theme)
	
	# === Button 样式 ===
	_setup_buttons(theme)
	
	# === Label 样式 ===
	_setup_labels(theme)
	
	# === Panel 样式 ===
	_setup_panels(theme)
	
	# === ProgressBar 样式 ===
	_setup_progress_bars(theme)
	
	# === LineEdit 样式 ===
	_setup_line_edits(theme)
	
	return theme

# === 字体设置 ===
func _setup_fonts(theme: Theme):
	# 加载字体（需要先导入字体文件）
	var font_path = "res://assets/fonts/Inter-Regular.ttf"
	if ResourceLoader.exists(font_path):
		var font_file = load(font_path)
		theme.set_font("font", "", font_file)
	
	# 设置默认字体大小
	theme.set_font_size("font_size", "", TypographySystem.FONT_SIZE_BODY)

# === Button 样式 ===
func _setup_buttons(theme: Theme):
	# Primary Button（主按钮）
	var btn_primary_normal = _create_button_style(
		ColorPalette.BUTTON_PRIMARY_BG,
		ColorPalette.BORDER_ACCENT,
		8, 1
	)
	var btn_primary_hover = _create_button_style(
		ColorPalette.BUTTON_PRIMARY_HOVER,
		ColorPalette.INFO_EMPHASIS,
		8, 2,
		ShadowSystem.shadow_md()
	)
	var btn_primary_pressed = _create_button_style(
		ColorPalette.BUTTON_PRIMARY_ACTIVE,
		ColorPalette.INFO_MUTED,
		8, 1
	)
	var btn_primary_disabled = _create_button_style(
		ColorPalette.BG_ELEVATED,
		ColorPalette.BORDER_SUBTLE,
		8, 1
	)
	
	theme.set_stylebox("normal", "ButtonPrimary", btn_primary_normal)
	theme.set_stylebox("hover", "ButtonPrimary", btn_primary_hover)
	theme.set_stylebox("pressed", "ButtonPrimary", btn_primary_pressed)
	theme.set_stylebox("disabled", "ButtonPrimary", btn_primary_disabled)
	
	theme.set_color("font_color", "ButtonPrimary", ColorPalette.TEXT_INVERSE)
	theme.set_color("font_hover_color", "ButtonPrimary", Color.WHITE)
	theme.set_color("font_pressed_color", "ButtonPrimary", ColorPalette.TEXT_INVERSE)
	theme.set_color("font_disabled_color", "ButtonPrimary", ColorPalette.TEXT_DISABLED)
	
	theme.set_font_size("font_size", "ButtonPrimary", TypographySystem.FONT_SIZE_BODY)
	
	# Secondary Button（次按钮）
	var btn_secondary_normal = _create_button_style(
		ColorPalette.BUTTON_SECONDARY_BG,
		ColorPalette.BORDER_DEFAULT,
		8, 1
	)
	var btn_secondary_hover = _create_button_style(
		ColorPalette.BUTTON_SECONDARY_HOVER,
		ColorPalette.BORDER_ACCENT,
		8, 1,
		ShadowSystem.shadow_sm()
	)
	
	theme.set_stylebox("normal", "ButtonSecondary", btn_secondary_normal)
	theme.set_stylebox("hover", "ButtonSecondary", btn_secondary_hover)
	theme.set_stylebox("pressed", "ButtonSecondary", btn_secondary_normal)
	theme.set_stylebox("disabled", "ButtonSecondary", btn_primary_disabled)
	
	theme.set_color("font_color", "ButtonSecondary", ColorPalette.TEXT_PRIMARY)
	theme.set_color("font_hover_color", "ButtonSecondary", Color.WHITE)
	
	# Default Button（默认按钮，用于未指定类型的按钮）
	theme.set_stylebox("normal", "Button", btn_secondary_normal)
	theme.set_stylebox("hover", "Button", btn_secondary_hover)
	theme.set_stylebox("pressed", "Button", btn_secondary_normal)
	theme.set_stylebox("disabled", "Button", btn_primary_disabled)
	
	theme.set_color("font_color", "Button", ColorPalette.TEXT_PRIMARY)
	theme.set_font_size("font_size", "Button", TypographySystem.FONT_SIZE_BODY)

# === Label 样式 ===
func _setup_labels(theme: Theme):
	theme.set_color("font_color", "Label", ColorPalette.TEXT_PRIMARY)
	theme.set_font_size("font_size", "Label", TypographySystem.FONT_SIZE_BODY)
	
	# Heading1
	theme.set_color("font_color", "LabelH1", ColorPalette.TEXT_PRIMARY)
	theme.set_font_size("font_size", "LabelH1", TypographySystem.FONT_SIZE_H1)
	
	# Heading2
	theme.set_color("font_color", "LabelH2", ColorPalette.TEXT_PRIMARY)
	theme.set_font_size("font_size", "LabelH2", TypographySystem.FONT_SIZE_H2)
	
	# Secondary text
	theme.set_color("font_color", "LabelSecondary", ColorPalette.TEXT_SECONDARY)
	theme.set_font_size("font_size", "LabelSecondary", TypographySystem.FONT_SIZE_BODY_SMALL)
	
	# Caption
	theme.set_color("font_color", "LabelCaption", ColorPalette.TEXT_TERTIARY)
	theme.set_font_size("font_size", "LabelCaption", TypographySystem.FONT_SIZE_CAPTION)

# === Panel 样式 ===
func _setup_panels(theme: Theme):
	# PanelContainer - 卡片容器
	var panel_card = _create_panel_style(
		ColorPalette.BG_ELEVATED,
		ColorPalette.BORDER_DEFAULT,
		6, 1,
		ShadowSystem.shadow_sm()
	)
	theme.set_stylebox("panel", "PanelContainer", panel_card)
	
	# Panel - 基础面板
	var panel_base = _create_panel_style(
		ColorPalette.BG_SURFACE,
		ColorPalette.BORDER_SUBTLE,
		4, 1
	)
	theme.set_stylebox("panel", "Panel", panel_base)
	
	# PanelElevated - 抬升面板
	var panel_elevated = _create_panel_style(
		ColorPalette.BG_OVERLAY,
		ColorPalette.BORDER_STRONG,
		8, 2,
		ShadowSystem.shadow_md()
	)
	theme.set_stylebox("panel", "PanelElevated", panel_elevated)

# === ProgressBar 样式 ===
func _setup_progress_bars(theme: Theme):
	# Background
	var progress_bg = StyleBoxFlat.new()
	progress_bg.bg_color = ColorPalette.BG_SURFACE
	progress_bg.corner_radius_top_left = 4
	progress_bg.corner_radius_top_right = 4
	progress_bg.corner_radius_bottom_left = 4
	progress_bg.corner_radius_bottom_right = 4
	progress_bg.border_width_all = 1
	progress_bg.border_color = ColorPalette.BORDER_SUBTLE
	
	# Fill（渐变效果）
	var progress_fill = StyleBoxFlat.new()
	progress_fill.bg_color = ColorPalette.SUCCESS_DEFAULT
	progress_fill.corner_radius_top_left = 4
	progress_fill.corner_radius_top_right = 4
	progress_fill.corner_radius_bottom_left = 4
	progress_fill.corner_radius_bottom_right = 4
	
	# 设置渐变（Godot 4.x）
	# 注：StyleBoxFlat的渐变需要通过shader实现
	# 这里使用纯色，如需渐变可在ProgressBar自定义shader
	
	theme.set_stylebox("background", "ProgressBar", progress_bg)
	theme.set_stylebox("fill", "ProgressBar", progress_fill)
	theme.set_color("font_color", "ProgressBar", Color.WHITE)
	theme.set_font_size("font_size", "ProgressBar", TypographySystem.FONT_SIZE_BODY_SMALL)

# === LineEdit 样式 ===
func _setup_line_edits(theme: Theme):
	var line_edit_normal = _create_panel_style(
		ColorPalette.BG_SURFACE,
		ColorPalette.BORDER_DEFAULT,
		4, 1
	)
	var line_edit_focus = _create_panel_style(
		ColorPalette.BG_ELEVATED,
		ColorPalette.INFO_DEFAULT,
		4, 2
	)
	
	theme.set_stylebox("normal", "LineEdit", line_edit_normal)
	theme.set_stylebox("focus", "LineEdit", line_edit_focus)
	theme.set_color("font_color", "LineEdit", ColorPalette.TEXT_PRIMARY)
	theme.set_color("font_placeholder_color", "LineEdit", ColorPalette.TEXT_TERTIARY)

# === 辅助方法 ===
func _create_button_style(
	bg_color: Color,
	border_color: Color,
	corner_radius: int = 8,
	border_width: int = 1,
	shadow: Dictionary = {}
) -> StyleBoxFlat:
	var style = StyleBoxFlat.new()
	style.bg_color = bg_color
	style.border_color = border_color
	style.border_width_all = border_width
	
	style.corner_radius_top_left = corner_radius
	style.corner_radius_top_right = corner_radius
	style.corner_radius_bottom_left = corner_radius
	style.corner_radius_bottom_right = corner_radius
	
	# 内边距
	style.content_margin_left = SpacingSystem.BUTTON_PADDING_H
	style.content_margin_right = SpacingSystem.BUTTON_PADDING_H
	style.content_margin_top = SpacingSystem.BUTTON_PADDING_V
	style.content_margin_bottom = SpacingSystem.BUTTON_PADDING_V
	
	# 阴影
	if shadow.has("shadow_color"):
		style.shadow_color = shadow.shadow_color
		style.shadow_size = shadow.shadow_size
		style.shadow_offset = shadow.shadow_offset
	
	return style

func _create_panel_style(
	bg_color: Color,
	border_color: Color,
	corner_radius: int = 6,
	border_width: int = 1,
	shadow: Dictionary = {}
) -> StyleBoxFlat:
	var style = StyleBoxFlat.new()
	style.bg_color = bg_color
	style.border_color = border_color
	style.border_width_all = border_width
	
	style.corner_radius_top_left = corner_radius
	style.corner_radius_top_right = corner_radius
	style.corner_radius_bottom_left = corner_radius
	style.corner_radius_bottom_right = corner_radius
	
	# 内边距
	style.content_margin_all = SpacingSystem.PANEL_PADDING
	
	# 阴影
	if shadow.has("shadow_color"):
		style.shadow_color = shadow.shadow_color
		style.shadow_size = shadow.shadow_size
		style.shadow_offset = shadow.shadow_offset
	
	return style

# === 保存主题 ===
func save_theme():
	var theme = build_theme()
	var err = ResourceSaver.save(theme, "res://themes/default_theme.tres")
	if err == OK:
		print("主题已保存到 res://themes/default_theme.tres")
	else:
		push_error("主题保存失败：", err)
```

### 在编辑器中生成主题

1. 将上述脚本附加到场景中的节点
2. 在编辑器中选中该节点
3. 在检查器中添加"Tool Script"
4. 调用 `save_theme()` 方法

或使用 EditorScript：

```gdscript
# themes/GenerateTheme.gd
@tool
extends EditorScript

func _run():
	var builder = ThemeBuilder.new()
	builder.save_theme()
	print("主题生成完成！")
```

---

## 3.2 重构 ThemeManager

```gdscript
# scripts/ui/ThemeManager.gd
class_name ThemeManager
extends Node

static var default_theme: Theme
static var current_theme: Theme
static var is_loaded: bool = false

# === 初始化 ===
static func initialize():
	if not is_loaded:
		load_theme()
		is_loaded = true

static func load_theme(path: String = "res://themes/default_theme.tres"):
	if ResourceLoader.exists(path):
		default_theme = load(path)
		current_theme = default_theme
		print("主题已加载：", path)
	else:
		push_error("主题文件不存在：", path)
		# 回退到代码生成的主题
		var builder = ThemeBuilder.new()
		default_theme = builder.build_theme()
		current_theme = default_theme

# === 应用主题 ===
static func apply_theme_to_node(node: Node):
	if node is Control:
		node.theme = current_theme

static func apply_theme_to_scene(scene: Node):
	_apply_recursive(scene)

static func _apply_recursive(node: Node):
	if node is Control and node.theme == null:
		node.theme = current_theme
	
	for child in node.get_children():
		_apply_recursive(child)

# === 获取颜色（向后兼容） ===
static func get_color(color_name: String) -> Color:
	match color_name:
		"background": return ColorPalette.BG_BASE
		"surface": return ColorPalette.BG_SURFACE
		"primary_text": return ColorPalette.TEXT_PRIMARY
		"secondary_text": return ColorPalette.TEXT_SECONDARY
		"success": return ColorPalette.SUCCESS_DEFAULT
		"warning": return ColorPalette.WARNING_DEFAULT
		"danger": return ColorPalette.DANGER_DEFAULT
		"info": return ColorPalette.INFO_DEFAULT
		_:
			push_warning("未知颜色名称：", color_name)
			return Color.WHITE

# === 获取 StyleBox ===
static func get_button_style(variant: String = "primary", state: String = "normal") -> StyleBoxFlat:
	var type_name = "Button" + variant.capitalize()
	return current_theme.get_stylebox(state, type_name)

static func get_panel_style(variant: String = "default") -> StyleBoxFlat:
	var type_name = "Panel" + ("" if variant == "default" else variant.capitalize())
	return current_theme.get_stylebox("panel", type_name)

# === 创建自定义样式 ===
static func create_card_style(is_distractor: bool = false) -> StyleBoxFlat:
	var style = StyleBoxFlat.new()
	
	if is_distractor:
		style.bg_color = Color("#15121 2")  # 略微偏红
		style.border_color = Color("#3A2525")
	else:
		style.bg_color = ColorPalette.CARD_BG_NORMAL
		style.border_color = ColorPalette.CARD_BORDER_NORMAL
	
	style.border_width_all = 1
	style.corner_radius_all = 6
	
	# 应用阴影
	var shadow = ShadowSystem.shadow_sm()
	style.shadow_color = shadow.shadow_color
	style.shadow_size = shadow.shadow_size
	style.shadow_offset = shadow.shadow_offset
	
	# 内边距
	style.content_margin_all = SpacingSystem.CARD_PADDING
	
	return style

static func create_slot_style(state: String = "empty") -> StyleBoxFlat:
	var style = StyleBoxFlat.new()
	
	match state:
		"empty":
			style.bg_color = ColorPalette.SLOT_BG_EMPTY
			style.border_color = ColorPalette.SLOT_BORDER_EMPTY
			style.draw_center = false  # 空槽只显示边框
		"filled":
			style.bg_color = ColorPalette.CARD_BG_NORMAL
			style.border_color = ColorPalette.CARD_BORDER_NORMAL
		"hover_valid":
			style.bg_color = ColorPalette.SLOT_BG_VALID
			style.border_color = ColorPalette.SLOT_BORDER_VALID
			style.border_width_all = 2
		"hover_invalid":
			style.bg_color = ColorPalette.SLOT_BG_INVALID
			style.border_color = ColorPalette.SLOT_BORDER_INVALID
			style.border_width_all = 2
	
	if state != "hover_valid" and state != "hover_invalid":
		style.border_width_all = 1
	
	style.corner_radius_all = 6
	style.content_margin_all = SpacingSystem.CARD_PADDING
	
	return style

# === 辅助扩展方法 ===
static func apply_stylebox_corner_radius(style: StyleBoxFlat, radius: int):
	style.corner_radius_top_left = radius
	style.corner_radius_top_right = radius
	style.corner_radius_bottom_left = radius
	style.corner_radius_bottom_right = radius
```

---

## 3.3 组件样式优化

### CauseCard 组件重构

```gdscript
# scripts/components/CauseCard.gd
class_name CauseCard
extends PanelContainer

signal drag_started(card: CauseCard)
signal drag_ended(card: CauseCard)

@export var cause_data: CauseNode

var is_dragging := false
var is_used := false
var original_position: Vector2

@onready var label = $MarginContainer/VBox/Label
@onready var tag_label = $MarginContainer/VBox/TagLabel

func _ready():
	_setup_visuals()
	mouse_entered.connect(_on_mouse_entered)
	mouse_exited.connect(_on_mouse_exited)

func _setup_visuals():
	# 使用主题样式
	var style = ThemeManager.create_card_style(cause_data.is_distractor)
	add_theme_stylebox_override("panel", style)
	
	# 设置文本
	label.text = cause_data.label
	label.add_theme_color_override("font_color", ColorPalette.TEXT_PRIMARY)
	label.add_theme_font_size_override("font_size", TypographySystem.FONT_SIZE_BODY)
	
	# 设置标签
	if cause_data.tags.size() > 0:
		tag_label.text = "# " + cause_data.tags[0]
		tag_label.add_theme_color_override("font_color", ColorPalette.TEXT_TERTIARY)
		tag_label.add_theme_font_size_override("font_size", TypographySystem.FONT_SIZE_CAPTION)
		tag_label.visible = true
	else:
		tag_label.visible = false

func _on_mouse_entered():
	if not is_used and not is_dragging:
		_apply_hover_effect()

func _on_mouse_exited():
	if not is_dragging:
		_remove_hover_effect()

func _apply_hover_effect():
	# 缩放动画
	var tween = create_tween()
	tween.set_parallel(true)
	tween.tween_property(self, "scale", Vector2(1.05, 1.05), 0.15).set_ease(Tween.EASE_OUT)
	
	# 更新样式
	var hover_style = ThemeManager.create_card_style(cause_data.is_distractor)
	hover_style.bg_color = ColorPalette.CARD_BG_HOVER
	hover_style.border_color = ColorPalette.CARD_BORDER_HOVER
	hover_style.border_width_all = 2
	
	# 添加光晕
	var shadow = ShadowSystem.shadow_md()
	hover_style.shadow_color = shadow.shadow_color
	hover_style.shadow_size = shadow.shadow_size
	hover_style.shadow_offset = shadow.shadow_offset
	
	add_theme_stylebox_override("panel", hover_style)

func _remove_hover_effect():
	var tween = create_tween()
	tween.tween_property(self, "scale", Vector2.ONE, 0.15).set_ease(Tween.EASE_OUT)
	
	# 恢复原样式
	var normal_style = ThemeManager.create_card_style(cause_data.is_distractor)
	add_theme_stylebox_override("panel", normal_style)

func _gui_input(event: InputEvent):
	if is_used:
		return
	
	if event is InputEventMouseButton:
		if event.button_index == MOUSE_BUTTON_LEFT:
			if event.pressed:
				_start_drag()
			else:
				_end_drag()

func _start_drag():
	is_dragging = true
	original_position = global_position
	z_index = 100
	
	# 拖拽状态视觉
	modulate.a = 0.8
	scale = Vector2(1.1, 1.1)
	
	drag_started.emit(self)

func _end_drag():
	is_dragging = false
	z_index = 0
	modulate.a = 1.0
	scale = Vector2.ONE
	
	drag_ended.emit(self)

func _process(_delta):
	if is_dragging:
		global_position = get_global_mouse_position() - size / 2

func set_used(used: bool):
	is_used = used
	
	if used:
		modulate = Color(0.5, 0.5, 0.5, 0.6)
	else:
		modulate = Color.WHITE

func return_to_original():
	var tween = create_tween()
	tween.set_parallel(true)
	tween.tween_property(self, "global_position", original_position, 0.3).set_ease(Tween.EASE_OUT).set_trans(Tween.TRANS_BACK)
	tween.tween_property(self, "scale", Vector2.ONE, 0.2)
```

### ChainSlot 组件重构

```gdscript
# scripts/components/ChainSlot.gd
class_name ChainSlot
extends PanelContainer

signal card_placed(slot: ChainSlot, card: CauseCard)
signal card_removed(slot: ChainSlot)

enum State { EMPTY, HOVER_VALID, HOVER_INVALID, FILLED }

var current_state := State.EMPTY
var current_card: CauseCard = null
var slot_index: int

@onready var placeholder_label = $CenterContainer/PlaceholderLabel
@onready var card_container = $CardContainer

func _ready():
	_update_visual()

func _update_visual():
	var state_name = _get_state_name()
	var style = ThemeManager.create_slot_style(state_name)
	add_theme_stylebox_override("panel", style)
	
	# 更新占位符
	match current_state:
		State.EMPTY:
			placeholder_label.visible = true
			placeholder_label.text = "?"
			placeholder_label.add_theme_color_override("font_color", ColorPalette.TEXT_TERTIARY)
		State.HOVER_VALID:
			placeholder_label.text = "✓"
			placeholder_label.add_theme_color_override("font_color", ColorPalette.SUCCESS_DEFAULT)
		State.HOVER_INVALID:
			placeholder_label.text = "✗"
			placeholder_label.add_theme_color_override("font_color", ColorPalette.DANGER_DEFAULT)
		State.FILLED:
			placeholder_label.visible = false

func _get_state_name() -> String:
	match current_state:
		State.EMPTY: return "empty"
		State.HOVER_VALID: return "hover_valid"
		State.HOVER_INVALID: return "hover_invalid"
		State.FILLED: return "filled"
		_: return "empty"

func can_accept_card(card: CauseCard) -> bool:
	# 检查是否重复
	var chain_area = get_parent()
	for slot in chain_area.get_children():
		if slot is ChainSlot and slot.current_card:
			if slot.current_card.cause_data.id == card.cause_data.id:
				return false
	return true

func place_card(card: CauseCard):
	if current_card:
		remove_card()
	
	current_card = card
	current_state = State.FILLED
	
	# 动画效果
	var card_clone = card.duplicate()
	card_clone.scale = Vector2(0.8, 0.8)
	card_clone.modulate.a = 0.0
	card_container.add_child(card_clone)
	
	var tween = create_tween()
	tween.set_parallel(true)
	tween.tween_property(card_clone, "scale", Vector2.ONE, 0.2).set_ease(Tween.EASE_OUT)
	tween.tween_property(card_clone, "modulate:a", 1.0, 0.2)
	
	card.set_used(true)
	_update_visual()
	card_placed.emit(self, card)

func remove_card():
	if current_card:
		current_card.set_used(false)
		current_card = null
		current_state = State.EMPTY
		
		# 清除动画
		for child in card_container.get_children():
			var tween = create_tween()
			tween.tween_property(child, "modulate:a", 0.0, 0.15)
			await tween.finished
			child.queue_free()
		
		_update_visual()
		card_removed.emit(self)

func _can_drop_data(_at_position, data) -> bool:
	if data is CauseCard:
		var valid = can_accept_card(data)
		current_state = State.HOVER_VALID if valid else State.HOVER_INVALID
		_update_visual()
		return valid
	return false

func _drop_data(_at_position, data):
	if data is CauseCard:
		place_card(data)
	current_state = State.FILLED if current_card else State.EMPTY
	_update_visual()

func _notification(what):
	if what == NOTIFICATION_DRAG_END:
		if current_state in [State.HOVER_VALID, State.HOVER_INVALID]:
			current_state = State.FILLED if current_card else State.EMPTY
			_update_visual()
```

---

## 3.4 场景文件应用主题

### 方法1：通过代码应用

```gdscript
# scenes/ui/MainMenu.gd
extends Control

func _ready():
	# 应用主题
	ThemeManager.initialize()
	ThemeManager.apply_theme_to_scene(self)
	
	# 为特定按钮指定主题类型
	$MenuButtons/StartButton.theme_type_variation = "ButtonPrimary"
	$MenuButtons/ContinueButton.theme_type_variation = "ButtonSecondary"
```

### 方法2：在编辑器中设置

1. 打开场景（如 `MainMenu.tscn`）
2. 选中根节点
3. 在检查器中找到 `Theme` 属性
4. 拖拽 `default_theme.tres` 到该属性

---

# 🎬 四、动效设计系统

## 4.1 过渡动画规范

```gdscript
# themes/AnimationSystem.gd
class_name AnimationSystem
extends Resource

# === 时长标准 ===
const DURATION_INSTANT = 0.0
const DURATION_FAST = 0.15      # 快速反馈
const DURATION_NORMAL = 0.3     # 标准过渡
const DURATION_SLOW = 0.5       # 慢速强调
const DURATION_VERY_SLOW = 0.8  # 超慢展示

# === 缓动函数 ===
const EASE_OUT_STANDARD = Tween.EASE_OUT
const EASE_IN_OUT_SMOOTH = Tween.EASE_IN_OUT
const TRANS_BACK = Tween.TRANS_BACK      # 回弹效果
const TRANS_ELASTIC = Tween.TRANS_ELASTIC # 弹性效果

# === 预定义动画 ===
static func fade_in(node: CanvasItem, duration: float = DURATION_NORMAL):
	node.modulate.a = 0.0
	var tween = node.create_tween()
	tween.tween_property(node, "modulate:a", 1.0, duration).set_ease(EASE_OUT_STANDARD)

static func fade_out(node: CanvasItem, duration: float = DURATION_NORMAL):
	var tween = node.create_tween()
	tween.tween_property(node, "modulate:a", 0.0, duration).set_ease(EASE_OUT_STANDARD)

static func scale_bounce(node: Control, target_scale: Vector2 = Vector2.ONE, duration: float = DURATION_NORMAL):
	var tween = node.create_tween()
	tween.tween_property(node, "scale", target_scale, duration).set_ease(EASE_OUT_STANDARD).set_trans(TRANS_BACK)

static func slide_in_from_bottom(node: Control, duration: float = DURATION_NORMAL):
	var start_pos = node.position
	node.position.y += 100
	node.modulate.a = 0.0
	
	var tween = node.create_tween()
	tween.set_parallel(true)
	tween.tween_property(node, "position", start_pos, duration).set_ease(EASE_OUT_STANDARD)
	tween.tween_property(node, "modulate:a", 1.0, duration * 0.6)

static func pulse_glow(node: Control, color: Color, count: int = 1):
	var original_modulate = node.modulate
	var tween = node.create_tween()
	
	for i in range(count):
		tween.tween_property(node, "modulate", color, DURATION_FAST)
		tween.tween_property(node, "modulate", original_modulate, DURATION_FAST)
```

---

# 🎨 五、参考案例与最佳实践

## 5.1 优秀独立游戏UI参考

| 游戏                        | UI特点           | 可借鉴点         |
| --------------------------- | ---------------- | ---------------- |
| **Baba Is You**             | 极简、高对比度   | 清晰的视觉层次   |
| **Return of the Obra Dinn** | 单色+点缀色      | 强烈的风格化     |
| **Papers, Please**          | 文档式设计       | 功能性与美学结合 |
| **Slay the Spire**          | 卡牌UI、状态图标 | 信息密度处理     |
| **The Witness**             | 环境融入UI       | 沉浸式体验       |

## 5.2 Web游戏UI优化建议

```gdscript
# === 性能优化 ===
# 1. 减少实时阴影，使用预渲染
# 2. 限制同时播放的Tween数量
# 3. 使用对象池管理UI元素
# 4. 避免每帧更新样式

# === 响应式设计 ===
# 1. 使用相对布局（Anchor）
# 2. 字体大小根据屏幕调整
# 3. 触摸设备增大点击区域

# === 可访问性 ===
# 1. 颜色对比度符合WCAG标准
# 2. 提供文字缩放选项
# 3. 支持键盘导航
```

---

# 📦 六、完整实施清单

## Week 1: 基础主题系统

- [ ] 创建 `ColorPalette.gd`
- [ ] 创建 `TypographySystem.gd`
- [ ] 创建 `ShadowSystem.gd`
- [ ] 创建 `SpacingSystem.gd`
- [ ] 创建 `ThemeBuilder.gd` 并生成主题文件
- [ ] 重构 `ThemeManager.gd`

## Week 2: 组件样式优化

- [ ] 优化 `CauseCard.gd`
- [ ] 优化 `ChainSlot.gd`
- [ ] 创建 `AnimationSystem.gd`
- [ ] 应用主题到 `MainMenu.tscn`
- [ ] 应用主题到 `GameMain.tscn`

## Week 3: 细节打磨

- [ ] 添加所有过渡动画
- [ ] 优化拖拽反馈
- [ ] 添加粒子特效（可选）
- [ ] 创建自定义光晕Shader（可选）
- [ ] 性能测试与优化

## Week 4: 最终验收

- [ ] 检查所有场景UI一致性
- [ ] 跨浏览器测试
- [ ] 移动端适配测试
- [ ] 用户测试收集反馈
- [ ] 文档完善

---

**这套方案的核心优势**：

1. ✅ **系统化**：不是零散的样式，而是完整的设计系统
2. ✅ **可扩展**：新增组件直接复用主题
3. ✅ **高品质**：参考了成熟游戏和Web设计标准
4. ✅ **符合气质**：冷静、专业、研究型的视觉语言
5. ✅ **高性能**：针对Web优化，加载快速

立即开始实施，从基础系统开始！🚀